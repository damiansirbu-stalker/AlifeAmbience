--- _da_manifest: DiegeticAmbience identity. Pure data, no logic. Read by _da_init (the gate) and the validator (deps.manifest_structure).

MANIFEST = {
    name    = "DiegeticAmbience",
    version = "1.0.0-snapshot",
    xlibs   = "1.8.3",
}
